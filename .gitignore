node_modules
/.svelte-kit
/build
.DS_Store
.vercel
*.local

# Local-only self-improvement log — one line per `npm run gates` run. See
# AGENTS.md § Rules that execute. Never make this a CI artifact or upload it
# anywhere: the whole point is that it stays on the machine that produced it.
/.claude/logs
